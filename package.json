{
  "name": "anime-downloader-api",
  "version": "1.0.0",
  "description": "An API to fetch anime episode download links from Gogoanime.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && apt-get update && apt-get install -y google-chrome-stable"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "cheerio": "^1.0.0-rc.12",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "puppeteer-core": "^21.0.1"
  },
  "engines": {
    "node": ">=16"
  }
}
